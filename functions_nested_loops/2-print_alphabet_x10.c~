#include "main.h"
/**
 *print_alphabet - it uses _putchar to print alphabet
 *
 *Description: Parameter ch is used to print
 *
 * Parameter: ch
 *
 * Return: Always 0.
 */

void print_alphabet(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		_putchar(ch);
	}
	_putchar('\n');
}
