#include "main.h"
/**
 * _abs - Absolute
 *
 * Description: This function prints the sign of a number
 *
 * @num: num is the parameter
 *
 * Return: Always 0.
 */
int _abs(int num)
{
	if (num < 0)
	{
		return (-num);
	}
	else
	{
		return (num);
	}
}
