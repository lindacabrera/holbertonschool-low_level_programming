#include <stdio.h>
/**
 * main - "Programming is like building a multilingual puzzle"
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,");/*inline commmet*/
return (0);
}
