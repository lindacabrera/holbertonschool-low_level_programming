#include "main.h"

/**
 * _strlen - This report, by its very length, defends itself against risk...
 *
 * Description: This function that returns the length of a string.
 *
 * Return: length of a string.
 */
int _strlen(char *s)
{
	int a = 0;

	for (; s[a] != '\0'; a++)
	{
	}
	return (a);
}
