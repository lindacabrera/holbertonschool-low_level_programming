#include "main.h"

/**
 * _strlen - This report, by its very length, defends itself against risk...
 *
 * Return: length of a string.
 */
int _strlen(char *s)
{
	int a = 0;

	for (; s[a] != '\0'; a++)
	{
	}
	return (a);
}
