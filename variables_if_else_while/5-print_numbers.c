#include <stdio.h>
/**
 * main - Numbers
 *
 * Description: prints all single digit numbers of base 10 starting from 0
 *
 * Parameter: n
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int n = 0;
		for (n = 0; n <= 9; n++)
		{
		printf("%d", n);
		}
		putchar('\n');
return (0);
}
